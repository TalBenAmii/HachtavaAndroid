<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.comps.myapplication">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/hachtavalogo"
        android:label="@string/app_name"
        android:roundIcon="@drawable/hachtavalogo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Options" />
        <activity android:name=".Math" />
        <service android:enabled="true" android:name=".BackgroundSoundService" />
        <activity android:name=".HebrewOrEnglish" />
        <activity android:name=".SaveOrLoadWordList"></activity>
    </application>

</manifest>